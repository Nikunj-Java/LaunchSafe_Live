services:
  - type: web
    name: launchsafe-live
    runtime: python
    buildCommand: "python -m compileall ."
    startCommand: "python server.py --port $PORT"
    healthCheckPath: /health
    autoDeploy: true
